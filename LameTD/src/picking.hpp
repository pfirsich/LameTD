#ifndef _PICKING_HPP_
#define _PICKING_HPP_

#include <algorithm> // port2015: so eigen can use std::min, and std::max
#include <Eigen/Core>

namespace picking {
	Eigen::Vector2i update( );
}

#endif
